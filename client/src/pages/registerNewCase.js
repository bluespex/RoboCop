import React, { Component } from "react";
import RegisterNewCase from "../components/RegisterNewCase";

export default class registerNewCase extends Component {
  render() {
    return (
      <div>
        <RegisterNewCase />
      </div>
    );
  }
}
